# dsh-turn-notify 的 bundle 补丁层(作为 bundle 安装时使用)。
# 若用「绝对路径直接挂载」方式,请改用 README 里的单行 insert,不要用本文件。
- insert:
    - id: turn-notify
      name: dsh-turn-notify
      # 如需覆盖默认配置,取消注释并按需修改:
      # config:
      #   appName: 'DeepSeek Harness'
      #   fallbackAgent: 'Agent'
      #   notifyKinds: []            # 空数组 = 全部结束原因都通知
      #   cooldownMs: 2000
      #   silent: false
